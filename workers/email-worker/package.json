{
  "name": "@briefcast/email-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal Cloudflare Worker for email ingestion to R2",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "wrangler deploy --dry-run",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest watch"
  },
  "dependencies": {
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "@cloudflare/vitest-pool-workers": "^0.9.0",
    "@types/js-yaml": "^4.0.9",
    "typescript": "^5.3.0",
    "wrangler": "^3.100.0",
    "vitest": "^2.0.0"
  }
}
