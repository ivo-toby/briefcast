import { beforeAll, afterAll, afterEach } from 'vitest';

beforeAll(async () => {
  // Global test setup
});

afterEach(() => {
  // Cleanup after each test
});

afterAll(async () => {
  // Global test teardown
});
